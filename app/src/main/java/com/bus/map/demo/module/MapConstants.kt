package com.bus.map.demo.module

object MapConstants {
    var routeInfoNode = "routeInfo"
    val rootNode = "root"
    val sourceNode = "source"
    val destinationNode = "destination"
    val routeNode = "route"
    val durationNode = "duration"
    val distanceNode = "distance"
}